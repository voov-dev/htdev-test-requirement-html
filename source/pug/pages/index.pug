extends ../components/base/layout

block variables
  - const title = "Conecom - тестовое задание" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = "container"
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = false
  - const footerMods = ""
  - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks
    +about-us()
    +blockquote()
    +banner()
    +section-advantages()
    +section-stats()

block modals
